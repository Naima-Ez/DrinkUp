<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrinkUp"
        tools:targetApi="31">

        <!-- Splash Activity -->
        <activity
            android:name="com.example.drinkup.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DrinkUp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name="com.example.drinkup.onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.DrinkUp"/>

        <!-- Auth Activities -->
        <activity
            android:name="com.example.drinkup.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.DrinkUp"/>

        <activity
            android:name="com.example.drinkup.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.DrinkUp"/>

        <!-- Main Activity -->
        <activity
            android:name="com.example.drinkup.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.DrinkUp"/>

        <!-- WorkManager Worker -->
        <receiver
            android:name="androidx.work.impl.background.systemalarm.ConstraintProxy$BatteryNotLowProxy"
            android:enabled="false"
            android:exported="false"
            tools:replace="android:enabled">
            <intent-filter>
                <action android:name="android.intent.action.BATTERY_OKAY"/>
                <action android:name="android.intent.action.BATTERY_LOW"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
